<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quotes</title>

    <link rel="preload" href="https://picsum.photos/400/300" as="image" />
    <style>
      * {
        margin: 0;
        padding: 0;
      }

      body {
        /*height: 100vh;*/
        background-color: #d2eed2;
      }
      .container {
        padding-top: 20px;
        background-color: white;
        border-radius: 22px;
        height: 75vh;
        padding: 17px;
        width: 73vh;
        margin: 2% 33%;
      }
      .picture img{
        padding-top: 30px;
      }
      .header{
        display: flex;

      }
      .firstHalf{
        color: rgb(58, 250, 0);
      }
      .secondHalf{
        color: black;
      }

      

      .picture {
        position: relative;
        display: flex; /* Use flexbox to center the quote */
        justify-content: center; /* Center the quote horizontally */
        align-items: center; /* Center the quote vertically */
        width: 100%; /* Ensure the picture container spans the entire width */
        height: auto; /* Allow the height to adjust based on the content */
    }
    
    .quote {
        position: absolute;
        background-color: rgba(255, 255, 255, 0.8); /* Add a semi-transparent background to make the text more readable */
        padding: 10px; /* Add padding to the quote for better readability */
        max-width: 80%; /* Limit the width of the quote to 80% of the picture width */
        text-align: center; /* Center the text horizontally */
        z-index: 1; /* Ensure the quote is above the picture */
    }
    
    
    </style>
  </head>
  <body>
    <div class="container">
        <div class="header">

            <div class="firstHalf">
               <b> COVER </b>
            </div>
            <div class="secondHalf">
                <b><u>QUOTES</u></b>
            </div>
        </div>
        
      <div class="picture">
        <img src="https://picsum.photos/400/300" alt="" />
        <div class="quote">
          
          <% if (randomQuote) { %>
              <p><%= randomQuote.text %></p>
          <% } else { %>
              <p>No quote available</p>
          <% } %>
      </div>
      </div>
    </div>
  </body>
  
</html>
